services:
  - type: web
    name: stock-tracker
    runtime: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python -m gunicorn server:app --bind 0.0.0.0:$PORT
    healthCheckPath: /api/health
